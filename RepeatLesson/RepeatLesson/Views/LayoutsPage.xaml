<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:RepeatLesson.ViewModels"
             mc:Ignorable="d"
             x:Class="RepeatLesson.Views.LayoutsPage">
    <ContentPage.BindingContext>
        <viewModel:LayoutPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Absolute Layout" Command="{Binding CommandAbsoluteLayout}"/>
            <Button Text ="FlexLayout" Command="{Binding CommandFlexLayout}"></Button>
            <Button Text="Grid Layout" Command="{Binding CommandGridLayout}"></Button>
            <Button Text="Relative Layout" Command="{Binding CommandRelativeLayout}"></Button>
            <Button Text="StackLayout" Command="{Binding CommandStackLayout}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>